{{- if or .Values.cvm.jkg.enabled .Values.global.core.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-db-configmap
data:
{{- range $key, $value := .Values.datagrok_test.database.configmap }}
  {{ $key }}: "{{ $value }}"
{{- end }}
{{- end }}