{{- if .Values.global.core.enabled -}}

{{- if .Values.datagrok_test.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.datagrok_test.container.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "base.defaultLabels" . | nindent 4 }}
  {{- with .Values.datagrok_test.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.datagrok_test.ingress.tls }}
  tls:
    {{- range .Values.datagrok_test.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{ $subdomain := .Values.datagrok_test.container.tag | replace "." "-" }}
    {{- range .Values.datagrok_test.ingress.hosts }}
    - host: "{{ $subdomain }}.{{ .host }}"
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .backend.serviceName }}
                port:
                  name: {{ .backend.servicePort }}
          {{- end }}
    {{- end }}
  {{- end }}
{{- end }}